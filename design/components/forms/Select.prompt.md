Popup button for settings choices (engine per function, language, retention).

```jsx
<Select mono options={["parakeet-tdt-0.6b-v3", "whisper-large-v3-turbo"]} />
```

Use `mono` whenever the value is a machine identifier.
