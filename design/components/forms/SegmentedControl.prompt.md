Switches between sibling views or short exclusive options (Notas / Transcript / Resumo, PT / EN, Claro / Escuro).

```jsx
<SegmentedControl options={["Notas", "Transcript", "Resumo"]} value={tab} onChange={setTab} />
```

Two to four options, one or two words each; use `Select` beyond that.
