The dictation HUD: a non-activating floating pill, bottom-center of the screen, over whatever app has focus.

```jsx
<DictationPill state="listening" target="Slack · casual" />
<DictationPill state="blocked" lang="pt" />
```

It is the only surface besides the menu-bar dropdown allowed to use vibrancy. Copy is one or two words.
