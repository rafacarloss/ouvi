Renders one Lucide glyph in the current text color; use it anywhere Ouvi needs an icon instead of hand-writing SVG.

```jsx
<Icon name="mic" size={16} />
```

Stroke weight comes from Lucide (1.5). Size 16 in chrome, 20 in HUD/empty states. Never use emoji instead.
