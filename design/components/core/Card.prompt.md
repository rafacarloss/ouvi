Generic surface for grouped content: session summaries, settings groups, site feature blocks.

```jsx
<Card padding="lg">…</Card>
<Card inset padding="sm">…</Card>
```

10px radius, hairline ring, flat fill. Never add a colored left border or a gradient.
