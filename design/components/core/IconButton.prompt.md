Label-less square control for window toolbars, transcript rails and list rows.

```jsx
<IconButton icon="search" title="Buscar" />
<IconButton icon="audio-lines" title="Mostrar transcript" active />
```

Never ship one without `title`. Use `active` for panel toggles.
