Section switch inside a panel; the underline is the only accent.

```jsx
<Tabs tabs={["Reuniões", "Pessoas", "Empresas"]} value={tab} onChange={setTab} />
```

For 2–4 short exclusive options inside a toolbar, prefer `SegmentedControl`.
