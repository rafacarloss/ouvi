The window's 44px control strip; holds the session title, search and panel toggles.

```jsx
<Toolbar leading={<strong>Superior Grocers</strong>} trailing={<IconButton icon="audio-lines" title="Transcript" active />}>
  <SearchField shortcut={["⌘","K"]} />
</Toolbar>
```
