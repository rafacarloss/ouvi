Short confirmation anchored bottom-trailing inside the window.

```jsx
<Toast tone="success" action={<Button variant="ghost" size="sm">Abrir</Button>}>Nota salva em ~/Ouvi/2026/08</Toast>
```

One sentence; never stack more than two.
