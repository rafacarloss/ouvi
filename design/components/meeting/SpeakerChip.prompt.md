Labels who is talking in the transcript rail and in speaker settings.

```jsx
<SpeakerChip name="Você" slot={0} />
<SpeakerChip name="Falante 2" slot={2} unnamed onName={openNaming} />
```

Color is a 3px rule, never the text color. The user's own channel is always slot 0.
