Appears on every session header, summary block and chat answer to state where the work ran.

```jsx
<PrivacyBadge mode="local" detail="transcrição e busca neste Mac" />
<PrivacyBadge mode="cloud" detail="resumo pela Claude API — só texto" />
```

Copy is factual, never boastful. Amber is used nowhere else in the app.
